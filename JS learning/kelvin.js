// Making a variable and asining it to 293K
var kelvin = 0;
var celsius = kelvin - 273;
var fahrenheit = Math.floor(celsius * (9 / 5) + 32);
console.log(`The temperature is ${fahrenheit} degrees Fahrenheit.`);
var newton = Math.floor(celsius * (33 / 100));
console.log(`The temperature is ${newton} Newton scale.`);
console.log(`The temperature is ${celsius} degrees Celsies`);
var print = function(print2) {
    console.log(print2)
}